export function Footer() {
    return (
        <footer className="border-t border-[#27272A] px-6 py-8 text-center text-sm text-zinc-500">
            © {new Date().getFullYear()} JobSphere AI — Built as a take-home project.
        </footer>
    );
}